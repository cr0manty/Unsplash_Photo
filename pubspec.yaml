name: unsplash_photo
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  http:
  http_interceptor:
  photo_view:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
